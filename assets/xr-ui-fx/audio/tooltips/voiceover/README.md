# Voiceover drop folder

Use the tooltip ID as the basename, for example `account-access.wav`. Keep each
read concise (roughly 12–30 seconds), conversational, and free of operational
claims that are not present in the visible interface.
